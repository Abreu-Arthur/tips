<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Tips</title>
    <style>
         body{
            display:flex;
            flex-direction: column;
            align-items: center;
            background-color: #000;
        }
        #box{
            width:450px;
            height:480px;
            border:1px solid #fff;
            margin-top: 50px;
            border-radius: 20px;
            padding:15px;
            box-sizing: border-box;
        }
        #todo{
            width:400px;
            height:170px;
            border:1px solid #fff;
            border-radius: 20px;
            resize: none;
            padding: 10px;
            box-sizing: border-box;
        }
        #imp,#day,#hour{
            width:400px;
            height:30px;
            border:1px solid #fff;
            border-radius: 10px;
            text-align: center;
            font-size: 15px;
            margin-top: 10px;
            transition: height 2s
        }
        #sub{
            width:400px;
            height:40px;
            border:1px solid #fff;
            border-radius: 10px;
            text-align: center;
            font-size: 15px;
            margin-top: 10px;
            color:#fff;
            background-color: #000;
        }
        #sub:hover{
            color:#000;
            background-color: #fff;
            box-shadow: 20px 20px 90px #999;
        }
    </style>
</head>
<body>
    <div id="box">
        <form action="/edited" method="post">
            <input type="hidden" name="id" value="{{todo._id}}">
            <label>TODO:</label><br>
            <textarea name="todo" id="todo"  onmouseover="corzin(this)">{{todo.todo}}</textarea><br>
            <label>Relevance:</label><br>
            <input type="number" id="imp" name="imp" value="{{todo.imp}}" onmouseover="corzin(this)"><br>
            <label>Dia:</label><br>
            <input type="number" id="day" name="dia" value="{{todo.dia}}" onmouseover="corzin(this)"><br>
            <label>Horário:</label><br>
            <input type="hour" name="hora" id="hour" value="{{todo.hora}}" onmouseover="corzin(this)"><br>
            <button type="submit" id="sub">Update</button>
        </form>
    </div>
    <script>
        function corzin(obj){
            var r=Math.floor(Math.random()*255)
            var g=Math.floor(Math.random()*255)
            var b=Math.floor(Math.random()*255)
            obj.style.backgroundColor='rgb('+r+','+g+','+b+')'
        }
    </script>
</body>
</html>