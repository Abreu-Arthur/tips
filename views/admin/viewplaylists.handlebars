<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My playlists</title>
    <style>
        body{
            background-color: #000;
            display: flex;
            flex-direction: column;
            align-items: center;
            color:#fff;
            perspective: 1000px;
        }
        #box{
            height:auto;
            width:850px;
            border:1px solid #fff;
            border-radius: 8px;
            text-align: left;
            background-color: #000;
            overflow: auto;
            color:#fff;
            padding-left: 10px;
            padding-top: 10px;
            margin-top:50px;
            transition:transform 1s
        }
        #box:hover{
            transform:rotateY(45deg)
        }
        #create,#see{
            width:200px;
            height:30px;
            border:1px solid #fff;
            border-radius: 10px;
            color:#fff;
            background-color: #000;
        }
    </style>
</head>
<body>
    <h1>Your playlists</h1>
    {{#each playlists}}
        <div id="box">
            <b>{{nome}}</b><br>
            <p>{{desc}}</p>
            <a href="/newitem/{{_id}}" ><button id="create">Create item</button></a>
            <a href="/itens/{{_id}}"><button id="see">Ver itens</button></a>
        </div>
    {{/each}}
</body>
</html>